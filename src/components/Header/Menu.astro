---
const menus = ["Event", "Speakers", "Partners"];
---

<ul
  class="mt-4 flex flex-col rounded-lg border border-gray-100 p-4 font-medium dark:border-gray-700 md:mt-0 md:flex-row md:space-x-8 md:border-0 md:p-0 rtl:space-x-reverse"
>
  {
    menus.map((menu) => (
      <li>
        <a href="#" class="text-white">
          {menu}
        </a>
      </li>
    ))
  }
</ul>
